# Journal de bord — brief online M5

| Date | Travail | Preuve | Décision | Risque ou question ouverte |
|---|---|---|---|---|
| 07/09 | Choix de l'artefact à déployer | candidat M4 gelé le 31/08, `results/gel/candidat_m4.joblib` | Déployer le détecteur de provenance plutôt qu'un modèle jetable : il a une model card, un gel daté, des métriques hors pli et une baseline | Le brief autorise « un modèle simple fourni » — celui-ci est mieux documenté que ce qu'on aurait fabriqué |
| 07/09 | Copie de l'artefact et vérification d'identité | empreinte de la copie = `164d05b129ce5e41…`, **identique à celle de la model card M4** | Le checksum devient un contrôle exécuté au démarrage, pas une ligne de documentation | — |
| 07/09 | Extraction du chemin d'inférence | `src/features.py` repris de `modele.py` **sans** le code d'entraînement (ni `candidats()`, ni RandomForest, ni partition) | Une image d'inférence n'embarque pas le code qui ne s'exécutera jamais : moins à maintenir, à auditer, à mettre au SBOM | Premier essai incomplet : coupé trop haut, `PLAGES` et les constantes du contrat capteur manquaient — rattrapé |
| 07/09 | **Vérification d'équivalence des features** | 30 fenêtres × 19 features recalculées et comparées à `results/modele/features.csv` de M4, tolérance 10⁻⁹ : **zéro écart** | L'équivalence est prouvée, pas supposée | C'est le contrôle central : une dérive d'extraction rendrait le modèle **silencieusement faux** — il prédirait bien, sur les mauvaises entrées |
| 07/09 | Environnement aligné sur le gel | venv 3.12 avec scikit-learn **1.7.1**, numpy 2.3.2, pandas 2.3.1 | La version de sklearn est traitée comme une unité versionnée, vérifiée au démarrage et exposée en métrique | Monter sklearn ne change ni le code ni le modèle, et peut changer les prédictions — le piège du registre de versions |
| 07/09 | Avertissement sklearn corrigé | « X has feature names, but StandardScaler was fitted without feature names » à chaque appel | Passage d'un tableau nu au lieu d'un DataFrame nommé ; l'ordre reste imposé par `gel.json` | Un warning à chaque prédiction aurait fini par masquer un vrai signal dans les journaux |
| 07/09 | API instrumentée | `/health/live`, `/health/ready`, `/version`, `/predict`, `/metrics` — 18 tests | `ready` exige modèle chargé **et** identifié **et** conforme. `live` n'en dépend pas, sinon l'orchestrateur redémarre en boucle | Chaque réponse de `/predict` porte le checksum du modèle : une prédiction sans artefact identifiable ne s'explique ni ne se rejoue |
| 07/09 | Stack conteneurisée | `scoring` (8100) + `prometheus` (9190), tous deux `healthy`, cible `up` | `retries: 2` sur le healthcheck — application de la remédiation n°4 du rapport de rollback présentiel | Ports décalés de la stack RAG pour coexistence |
| 07/09 | **Reproductibilité poste ↔ conteneur** | fenêtre `M4-CAL-001` → `0.114902` en local **et** en conteneur | Chiffre identique, pas « du même ordre » | — |
| 07/09 | **Livraison d'un candidat défectueux** | artefact corrompu (10 octets réécrits) : les tests échouent (`Artefact non conforme : 630004cc…`), et le conteneur passe **`unhealthy` en 33 s** avec Prometheus qui ne démarre pas | Deux barrières indépendantes | La seconde n'est pas une redondance de confort : un artefact peut être remplacé **après** la CI. Le démarrage est le seul contrôle qui porte sur ce qui tourne |
| 07/09 | **Rollback exécuté** | retour au tag `v1-sain` : service rétabli en **7 s**, checksum sain, fenêtre témoin à `0.114902` **exactement** | La vérification rejoue la prédiction, pas seulement la readiness | `build: !reset null` indispensable — sans lui Compose reconstruit et réintroduit le défaut |
| 07/09 | Documentation des livrables | `artefact.md`, `registre_versions.md`, `metriques_et_declencheurs.md`, `rapport_livraison.md`, `rollback.md` | Chaque métrique porte source, fréquence, seuil, destinataire et action | **Trois trous assumés** : pas de dérive de distribution surveillée, un seul environnement (distinction préprod/prod structurelle mais pas topologique), pas de registre d'images |
| 07/09 | CI dédiée | `.github/workflows/diagops-m5-online.yml` — 2 jobs : artefact+équivalence, puis stack+fumée | La CI rejoue la prédiction témoin et **échoue si la probabilité diffère** | Autonome de la chaîne RAG : autre artefact, autres dépendances, autre image |
