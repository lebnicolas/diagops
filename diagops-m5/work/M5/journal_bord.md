# Journal de bord M5

| Date | Travail | Preuve | Décision | Risque ou question ouverte |
|---|---|---|---|---|
| 07/09 | Ouverture du paquet M5 et contrôle d'intégrité de la référence | `tools/check_m5_release.py` → `status: ready` : 6 documents, 27 fichiers starter, 14 fichiers de référence, 81 fichiers data pack, hit@3 = 1,00 | Référence `diagops-m4-reference-r1` acceptée comme état de départ | L'outil échoue sous Windows (chemins `\` contre `/` dans `checksums.sha256`) — patché localement en `as_posix()`, données intactes. Troisième écart Windows de la formation après M2 et M3. |
| 07/09 | Montage de l'environnement et exécution du starter | venv Python 3.12.10, `requirements.lock` ; `pytest` → 8 tests passés | Environnement aligné sur celui de M4 | Aucun test du starter ne dépend de la plateforme, contrairement à M3 |
| 07/09 | Reconstruction de l'index de référence | `build_index.py` → 7 documents, `index_version` = `lexical-dfb8faf0c9b4` | Reproductibilité confirmée : **version identique à la référence distribuée** | — |
| 07/09 | Exécution du gate de livraison | `evaluate_release.py` → `status: passed`, 4 checks verts | Chaîne nominale fonctionnelle | **Trois des quatre checks ne lisent pas le candidat** — ils lisent `metrics_calibration.json`, figé. Vérifié : un index aux checksums et token_count nuls passe le gate. À corriger avant toute promotion. |
| 07/09 | Branchement du gate sur le candidat — `pipelines/measure_release.py` | mesure rejouant le retrieval lexical M4 sur l'index candidat : hit@3 = 1,00, citations = 1,00, actifs restreints cités = 0 — **valeurs identiques à la référence M4**, l'implémentation est validée par reproduction | Séparation mesure / décision : `measure_release.py` mesure, `evaluate_release.py` applique les seuils (signature préservée, tests du starter intacts) | L'index ne portait pas de quoi chercher — ajout des **postings** (comptes de tokens), pas du texte : le document n'est pas reconstituable, le corpus n'est pas copié dans `work/M5` |
| 07/09 | Traçabilité de la construction d'index | ajout de `build_version` = `build-0c139fd0de6a` (stratégie + regex + checksum du pipeline) | `index_version` inchangée à dessein — la comparabilité avec la référence est préservée | Trou d'attribution n°1 comblé sans casser la référence |
| 07/09 | Vérification que le gate bloque réellement | index aux postings vidés : hit@3 tombe à **0,0**, gate `failed`, exit 1, promotion refusée. Métriques d'un autre index → check `metrics_match_index` false | Le gate mesure ce qu'il prétend mesurer | Le même index passait `passed` avant correction — trou n°2 comblé |
| 07/09 | `correct_abstention_rate` déclarée non mesurable | scores mesurés : RAG-CAL-008 (répondable) 0,33 **sous** RAG-CAL-012 (sans réponse) 0,36 — aucun seuil ne sépare | Métrique **héritée** de la référence et tracée `inherited:` dans le rapport de gate, plutôt que simulée | L'abstention appartient à la couche de génération, hors périmètre M5. À remesurer si une génération est introduite |
| 07/09 | Tests de non-régression | `tests/test_measure_release.py` — 6 tests ; suite complète **14 tests passés** | Le comportement corrigé est figé | — |
| 07/09 | Rédaction du contrat de versions (bloc 1) | `docs/contrat_versions.md` — 10 unités + 2 unités d'exécution | Unité 7 (modèle d'embeddings) déclarée **sans objet** : le retrieval de référence est lexical | **`index_version` ne couvre pas la stratégie de construction** — vérifié : la tokenisation passe de 780 à 446 tokens, la version reste `lexical-dfb8faf0c9b4`. La stratégie de chunking n'est traçable que par le checksum du pipeline. |
| 07/09 | Bloc 2 — service `gate` intercalé dans la stack | `deploy/compose.yaml` : `indexer` → `gate` → `api` → `prometheus`, chaînés par `service_completed_successfully` / `service_healthy` | L'index candidat est construit dans `candidates/`, et n'est copié vers `runtime/` **qu'après** un gate réussi | Le compose livré enchaînait `indexer` puis `api` sans évaluation : la stack servait un index que personne n'avait regardé — piège « reconstruire l'index directement en production » |
| 07/09 | Readiness branchée sur l'index réellement servi | index sain → 200 + `lexical-dfb8faf0c9b4` ; postings vidés → **503 « index actif sans postings exploitables »**, `diagops_index_valid 0` **sans toucher à `faults.json`** | `index_integrity()` observe l'artefact ; l'interrupteur d'incident ne peut que dégrader, jamais reverdir | `diagops_index_valid` était porté par `faults.json` seul : un index corrompu restait vert. Le scénario game day « index corrompu » n'aurait été vu que parce qu'annoncé |
| 07/09 | Durcissement des conteneurs | `read_only`, `cap_drop: ALL`, `no-new-privileges`, `/tmp` en tmpfs 64M, limites CPU/mémoire sur les 4 services, `start_period` et `stop_grace_period`, timeouts uvicorn, rétention Prometheus 7 j | Limites de ressources et timeouts exigés par le brief, absents du starter | — |
| 07/09 | Ajout d'un `.dockerignore` | absent du starter : `COPY . .` embarquait `.venv/`, `artifacts/`, `tests/` | Taille, reproductibilité (binaires Windows dans une image Linux) et surface d'exposition | — |
| 07/09 | Tests de non-régression bloc 2 | `tests/test_index_integrity.py` — 6 tests ; suite complète **20 tests passés** | Détection réelle figée, comportement du starter préservé quand `DIAGOPS_ACTIVE_INDEX` n'est pas défini | **Build Docker non exécuté** : Docker Desktop n'était pas démarré. `docker compose config` valide, la chaîne est vérifiée hors conteneur. À rejouer en conteneur avant de clore le bloc 2 |
| 07/09 | Bloc 2 — build et exécution de la stack conteneurisée | `docker compose up` : `indexer` (7 documents) → `gate` (`passed`, index publié) → `api` **healthy** → `prometheus` **healthy**, cible `diagops-api` en `up`, `diagops_ready` / `diagops_index_valid` / `diagops_dependency_up` scrappées à 1 | La chaîne des 4 services fonctionne bout en bout en conteneur | Deux incidents de mise en route : pull `auth.docker.io` coupé (réussi au second essai), et `--web.enable-lifecycle=false` refusé par kingpin — la forme négative est `--no-<flag>` |
| 07/09 | **Preuve que le gate bloque la mise en service** | gate recalé via un `minimum_document_count: 99` monté en override : `Gate failed`, exit 1 ; API restée en `Created`, **jamais démarrée** (curl → code 000) ; volume d'artefacts inspecté : **aucun dossier `runtime/`**, seul `candidates/compose/` existe | Un candidat recalé ne devient jamais l'index servi — le chaînage `service_completed_successfully` porte la garantie | Fichiers de test supprimés après la démonstration ; stack nominale relancée et saine |
| 07/09 | Relevé des digests d'images | `python:3.12.11-slim-bookworm` → `sha256:519591d6871b…` ; `prom/prometheus:v3.5.0` → `sha256:63805ebb8d2b…` ; images applicatives 230 Mo | Digests consignés au contrat de versions ; un tag mutable ne sert plus de version | Une seule image pour les 3 rôles applicatifs — un digest à suivre, pas trois. Reste à les figer dans `configs/release.json` à la première promotion |
| 07/09 | Bloc 3 — `pipelines/ingest.py` : contrat d'admission | validation étendue du manifeste — sensibilité, statut, rôles connus, cohérence des remplacements, checksum. Garde-fou : le corpus livré passe sans aucun refus | `build_index.py` ne vérifiait que le checksum, alors que le README du corpus exige aussi statut, sensibilité et rôles **avant** de rendre un document récupérable | Un document `restreint` ouvert au rôle `public` est désormais un refus, pas une tolérance |
| 07/09 | Détection ajout / modification / retrait / conflit de révision | `diff_against()` + 11 tests sur fixtures isolées | Trois issues seulement : `rejected`, `unchanged`, `rebuild` | **Le conflit qui compte** : contenu modifié à **révision constante** → `rejected`. Deux contenus différents porteraient sinon la même identité dans toutes les traces. Cas inverse (révision changée sans changement de contenu) signalé sans bloquer |
| 07/09 | Idempotence vérifiée en conteneur | 1er démarrage volume vierge → `rebuild`, +7 ajouts, gate passé, index publié. 2e démarrage corpus inchangé → `unchanged`, **aucune réindexation ni réévaluation**, API toujours `ready` sur `lexical-dfb8faf0c9b4` | Le gate lit le rapport d'ingestion et sort en no-op si rien n'a changé | Réévaluer à corpus constant republierait un index pour rien et donnerait une occasion d'échouer à une chaîne qui n'avait aucune raison de tourner |
| 07/09 | Bloc 5 — endpoint `/search` conforme au contrat de réponse | `POST /search` rend `answer` / `citations` / `abstained` ; technicien → 3 citations résolubles, rôle public sur sujet restreint → `abstained: true` | Les plans retrieval et réponse n'avaient **rien à observer** : l'API n'exposait aucune recherche | Pas de génération (aucun modèle dans le périmètre). Les extraits de citation sont des **références** (révision, tokens indexés), jamais le texte du document |
| 07/09 | Scoring extrait dans `src/retrieval.py` | `measure_release.py` et l'API importent la même fonction ; 42 tests passent après refactor | Un seul classement partagé | Deux implémentations divergentes auraient fait que le gate ne dit plus rien sur ce qui est réellement servi |
| 07/09 | Registre de métriques maison — `src/observability.py` | compteurs, gauges et histogrammes cumulatifs au format d'exposition Prometheus, sans nouvelle dépendance | `requirements.lock` inchangé : ajouter `prometheus_client` élargirait la surface à surveiller pour économiser 50 lignes | Bornes d'histogramme séparées : latences en secondes, comptages en unités — un top-k n'a rien à faire sur une échelle de millisecondes |
| 07/09 | Instrumentation des trois plans | **service** : requêtes par route/méthode/statut + latence. **retrieval** : requêtes, vides, filtrées par rôle, nombre de résultats, durée. **réponse** : refus, citations résolubles ou non, citations restreintes, erreurs | 15 métriques contre 3 gauges statiques au départ | Un service à 100 % de disponibilité qui cite des documents inexistants était vert avant : il ne l'est plus |
| 07/09 | Minimisation des traces, testée | test dédié : une question contenant un numéro et le mot « patient » n'apparaît **nulle part** dans l'exposition ; routes inconnues agrégées sous `other` | Aucune donnée métier dans un label — exigence du brief, et garde-fou de cardinalité | Une URL forgée deviendrait sinon une série temporelle : l'attaquant choisirait ce qu'on stocke |
| 07/09 | Contrat de métriques rempli — `monitoring/metrics.md` | 16 lignes avec source, fréquence, seuil, responsable et action | Deux seuils à **> 0 immédiat** : citation non résoluble, et document restreint cité hors rôle (incident de sécurité, pas de qualité) | **Trous assumés et documentés** : revue humaine et coût sans objet ici (M6), ressources conteneur non exposées par l'application — signalés plutôt que simulés à zéro |
| 07/09 | Vérification en conteneur | trafic réel envoyé à la stack ; Prometheus rend `retrieval_queries{public=1,technicien=1}`, `retrieval_empty{public=1}`, `refusals{no_evidence=1}`, `citations{resolvable=true}=3` | Les trois plans remontent bout en bout | p95 sur `/search` à `NaN` : trop peu de points dans la fenêtre de 5 min, attendu à ce volume |
